var N = 0, E = 1, S = 2, O = 3;

// IA d'un Jeremy
function Jeremy(x, y) {
	this.orientation = N;
	this.visionField = 3;
	// On place par défaut Jeremy à l'arrivée du laby
	this.x = _tailleMax - 2;
	this.y = _tailleMax - 1;
	
	this.canWalk = function(map, x, y) {
		//if(map[x][y] == )
	}
}